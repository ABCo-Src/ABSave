name: '[Standard 2.1] Build & Test'

on: 
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '3.1.x'

      - name: Restore Packages
        run: dotnet restore

      - name: Build
        run: dotnet build tests/ABCo.ABSave.UnitTests/ABCo.ABSave.UnitTests.csproj --framework netcoreapp3.1 --no-restore 
      
      - name: Run Tests
        run: dotnet test --no-build --configuration Release